/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.unsplash.com",
            "images.openai.com",
            "plus.unsplash.com"
        ],
    }
};

export default nextConfig;
